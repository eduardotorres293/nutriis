import 'package:flutter/material.dart';

class Searcher extends StatelessWidget {
  const Searcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Buscador', style: TextStyle(fontSize: 24)),
    );
  }
}