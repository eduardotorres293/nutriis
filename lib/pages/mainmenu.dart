import 'package:flutter/material.dart';

class Mainmenu extends StatelessWidget {
  const Mainmenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Menu principal', style: TextStyle(fontSize: 24)),
    );
  }
}